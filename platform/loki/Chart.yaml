apiVersion: v2
name: loki
description: >
  Observabilidad de la plataforma juan-in-one — Fase C: Loki (almacén de logs,
  el "Prometheus de los logs") vía el chart oficial de Grafana Labs. Wrapper
  como postgres/vault/monitoring, en vez de instalar el chart suelto a mano.
type: application
version: 0.1.0
dependencies:
  - name: loki
    version: "6.55.0" # empaqueta Loki 3.6.7
    repository: https://grafana.github.io/helm-charts
