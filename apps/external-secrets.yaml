apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: external-secrets
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://charts.external-secrets.io
    chart: external-secrets
    targetRevision: "2.9.0"
    helm:
      valuesObject:
        installCRDs: true # instala SecretStore/ExternalSecret la primera vez
  destination:
    server: https://kubernetes.default.svc
    namespace: external-secrets
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      # Las CRD SecretStore/ClusterSecretStore superan el límite de tamaño de
      # la anotación que usa el "apply" normal (mismo problema que al
      # instalar ArgoCD ayer) — server-side apply lo evita.
      - ServerSideApply=true
